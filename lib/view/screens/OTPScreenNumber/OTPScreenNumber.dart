import 'package:flutter/material.dart';

class OTPScreenNumber extends StatelessWidget {
  const OTPScreenNumber({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(
        child: Center(
          child: Text('OTP Screen Number'),
        ),
      ),
    );
  }
}
