name: Al_Zab_township_guide
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1
environment:
  sdk: '>=3.4.4 <4.0.0'
 
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons:
  firebase_core:
  firebase_auth:
  email_otp: ^3.0.2
  cloud_firestore:
  provider:
  url_launcher: 
  shared_preferences:
  flutter_localizations:
    sdk: flutter
  sizer: 
  firebase_database: 
  device_preview: ^1.2.0
  intl: ^0.19.0
  # flutter_launcher_icons: ^0.13.1


dev_dependencies:
  flutter_test:
    sdk: flutter
   


  flutter_lints: ^4.0.0

flutter:
  generate: true

  uses-material-design: true
  
  assets:
    - assets/

flutter_intl:
  enabled: true
